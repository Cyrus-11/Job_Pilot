import type { ReactElement } from "react";

import { PendingPage } from "@/components/layout/PendingPage";

export default function FindJobsPage(): ReactElement {
  return <PendingPage title="Find Jobs" message="Job search is not available yet. Please check back later." />;
}
